apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: chat-app
spec:
  type: LoadBalancer
  selector:
    app: frontend
    version: blue   # default
  ports:
  - port: 80
    targetPort: 80
